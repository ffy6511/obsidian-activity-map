import wordmarkDataUrl from '../../docs/assets/activity-map-wordmark.png';

/** The supplied wordmark is bundled as bytes so exported SVG has no runtime path dependency. */
export const POSTER_WORDMARK_DATA_URL = wordmarkDataUrl;
