# Execution Blockers and Autonomous Decisions

This newest-first log records unanticipated execution choices. Default to a safe, reversible decision inside the accepted Spec and continue. Use `awaiting-user` only when completion needs new authority, carries material irreversible risk, or changes accepted product or security semantics. This file is historical audit evidence, not the current blocker dashboard; read the owning Active Spec's Decision Summary for current state. It does not override Constitution, Architecture/docs, Roadmap, Research, or the owning Active Spec.

Insert each new entry immediately below the marker. Keep entries concise and never record secret values.

<!-- BLOCKED_ENTRIES_START -->
